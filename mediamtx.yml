# MediaMTX configuration (optimized for low-latency TCP RTSP streaming)

# Port to bind for RTSP streams
rtspAddress: :8554

# Restrict to TCP protocol only to prevent UDP handshake delays and packet loss
protocols: [tcp]

# Buffer count (default is 1024; reducing to 512 decreases latency)
readBufferCount: 512
